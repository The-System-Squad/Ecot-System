import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ecot.food.ordering.system',
  appName: 'ecot-food-ordering-system',
  webDir: 'www\browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
